[build]
rustc-wrapper = "sccache"
jobs = 6

# Disable incremental compilation to enable sccache caching across worktrees.
# Incremental builds are per-target-directory and can't be cached by sccache.
[env]
CARGO_INCREMENTAL = "0"

[alias]
ork = "run -p orkestra-cli --"
