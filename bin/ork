#!/usr/bin/env bash
set -e

# Find the repo root (where Cargo.toml workspace lives)
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec cargo run --manifest-path "$REPO_ROOT/Cargo.toml" -p orkestra-cli -- "$@"
