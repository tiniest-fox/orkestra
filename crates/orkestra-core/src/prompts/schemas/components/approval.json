{
  "description": "Approval decision with review content",
  "properties": {
    "decision": {
      "type": "string",
      "enum": ["approve", "reject"],
      "description": "Whether to approve or reject the work"
    },
    "content": {
      "type": "string",
      "description": "Review content: on approve this becomes the artifact, on reject this becomes feedback"
    },
    "activity_log": {
      "type": "string",
      "description": "Terse bullet points (2-5) covering: what you built, what you skipped (with reason), key decisions. Be specific, not generic. Focus on decisions and outcomes, not process."
    }
  },
  "required": ["decision", "content"]
}
