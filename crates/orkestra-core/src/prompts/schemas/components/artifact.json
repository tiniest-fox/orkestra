{
  "description": "Stage artifact output - the main deliverable",
  "properties": {
    "content": {
      "type": "string",
      "description": "The artifact content (usually markdown)"
    },
    "activity_log": {
      "type": "string",
      "description": "Terse bullet points (2-5) covering: what you did, what you skipped (with reason), key decisions. Be specific, not generic. Focus on decisions and outcomes, not process."
    }
  },
  "required": ["content"]
}
