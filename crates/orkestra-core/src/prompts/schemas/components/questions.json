{
  "description": "Clarifying questions before proceeding",
  "properties": {
    "questions": {
      "type": "array",
      "description": "Questions that need answers",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier (e.g., q1, q2)"
          },
          "question": {
            "type": "string",
            "description": "The question text"
          },
          "context": {
            "type": "string",
            "description": "Why this question matters"
          },
          "options": {
            "type": "array",
            "description": "Available answer options",
            "items": {
              "type": "object",
              "properties": {
                "label": {
                  "type": "string",
                  "description": "Short display label"
                },
                "description": {
                  "type": "string",
                  "description": "Explanation of this choice"
                }
              },
              "required": ["label"]
            }
          }
        },
        "required": ["id", "question", "options"]
      },
      "minItems": 1
    }
  },
  "required": ["questions"]
}
