{
  "description": "Breakdown into subtasks",
  "properties": {
    "content": {
      "type": "string",
      "description": "The artifact content (technical design, analysis, research findings as markdown)"
    },
    "subtasks": {
      "type": "array",
      "description": "List of subtasks to create (empty array with skip_reason to skip breakdown)",
      "items": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Short title for the subtask"
          },
          "description": {
            "type": "string",
            "description": "Brief summary of what this subtask accomplishes (1-2 sentences)"
          },
          "detailed_instructions": {
            "type": "string",
            "description": "Detailed implementation instructions for this specific subtask (becomes the subtask's primary artifact)"
          },
          "depends_on": {
            "type": "array",
            "description": "Indices of subtasks this depends on (0-based)",
            "items": { "type": "integer" }
          }
        },
        "required": ["title", "description", "detailed_instructions"]
      },
      "minItems": 0
    },
    "skip_reason": {
      "type": "string",
      "description": "Required justification when subtasks array is empty (skipping breakdown)"
    },
    "activity_log": {
      "type": "string",
      "description": "Terse bullet points (2-5) covering: decisions made and their rationale, significant findings that inform future stages, failed approaches ONLY when they informed a different decision. Do NOT include: routine exploration or tool use details, step-by-step implementation narrative, information already captured in the artifact content."
    }
  },
  "required": ["content", "subtasks"]
}
