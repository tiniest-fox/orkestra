<!orkestra-resume>
{{#if integration_error}}
## MERGE CONFLICT - Previous Integration Failed

Your previous work was completed but failed to merge into the main branch.

**Error:** {{{integration_error}}}

{{#if conflict_files}}
**Conflicting files:**
{{#each conflict_files}}
- {{this}}
{{/each}}
{{/if}}

Please resolve the merge conflict:
1. Run `git fetch origin main && git rebase origin/main`
2. Resolve any conflicts in the listed files
3. Complete the rebase with `git add . && git rebase --continue`
4. Verify your changes still work correctly
5. Output your completion status as JSON (see initial prompt for format)

{{else if review_feedback}}
{{{review_feedback}}}

Please address the above and output your completion status as JSON (see initial prompt for format).
{{else}}
The session was interrupted. Please continue implementing the task where you left off.

When done, output your completion status as JSON (see initial prompt for format).
{{/if}}
