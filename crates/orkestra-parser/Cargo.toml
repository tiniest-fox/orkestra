[package]
name = "orkestra-parser"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
orkestra-types = { path = "../orkestra-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
jsonschema = "0.26"
thiserror = "1"
