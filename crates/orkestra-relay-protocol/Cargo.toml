[package]
name = "orkestra-relay-protocol"
version = "0.1.1"
edition = "2021"
description = "Wire protocol types for the Orkestra relay"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
